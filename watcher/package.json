{
  "name": "@candence/watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Candence WebSocket fallback watcher — offchain failover ONLY, never the decision path (DIRECTIVE §4.5).",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint) @candence/watcher\""
  },
  "dependencies": {
    "@candence/shared": "workspace:*",
    "@somnia-chain/markets-sdk": "^0.25.0",
    "dotenv": "^16.4.5",
    "viem": "^2.17.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/ws": "^8.5.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
